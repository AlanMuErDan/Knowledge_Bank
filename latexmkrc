$pdf_mode = 1;

# Keep LaTeX build artifacts out of the project root.
$out_dir = 'build';

# Extend cleanup to common auxiliary files.
$clean_ext = 'aux bbl bcf blg fdb_latexmk fls log lof lot out run.xml synctex.gz toc';
